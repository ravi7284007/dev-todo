touch .gitignore - for ignore file
git config --global user.name Ravi Singh
git config --global user.email "ravi@gmail.com"
git init - for start
git add . - stagging area
git commit -m 'Initial commit' - Comments for changes

git status - just to see changes

For Genrate SSH Key -
ssh-keygen -t rsa -b 4069 -C "ravi7284007@gmail.com"

tail <path to id_rsa.pub> #Add this key your GitHub account
git remote add origin <ssh url.git>
git push origin master